# Database provider: 'neon' (default) or 'supabase'
VITE_DB_PROVIDER=neon

# Neon — use a READ-ONLY role's connection string (never the owner role)
VITE_NEON_DATABASE_URL=postgresql://loldle_read:password@your-endpoint-pooler.region.aws.neon.tech/neondb?sslmode=require

# Supabase (fallback provider, switch via VITE_DB_PROVIDER=supabase)
VITE_SUPABASE_URL=https://wujckwchvygpzlltfmbm.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
